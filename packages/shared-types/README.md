# Shared Types

Shared domain types for normalized sports data and public API contracts.

Provider-specific types belong near provider ingestion code unless they are intentionally part of a debug-only contract.
