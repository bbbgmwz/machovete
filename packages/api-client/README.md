# API Client

Shared typed API client for web, iOS, and Android contract parity.

The first implementation may generate TypeScript clients first, but mobile clients must follow the same contract semantics.
